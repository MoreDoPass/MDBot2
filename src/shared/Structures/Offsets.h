// (Новый файл) src/shared/Structures/Offsets.h

#pragma once

#include <cstdint>

/**
 * @brief Пространство имен для хранения статических адресов и смещений памяти клиента игры.
 *
 * Централизованное хранение этих констант упрощает обновление и поддержку бота
 * при выходе новых патчей клиента. Использование constexpr гарантирует,
 * что все значения будут вычислены на этапе компиляции, не создавая
 * лишних переменных в памяти.
 * Этот файл является частью "знаний" DLL о внутреннем устройстве клиента.
 */
namespace Offsets
{
/**
 * @brief Адрес битовой маски состояния рун Рыцаря Смерти.
 * @details Тип данных в памяти: DWORD (uint32_t). Биты 0-5 отвечают за готовность 6 рун (1 = готова, 0 = на КД).
 *          - Биты 0, 1: Руны Крови
 *          - Биты 2, 3: Руны Нечестивости
 *          - Биты 4, 5: Руны Льда
 */
constexpr uintptr_t RUNE_STATUS_MASK_ADDR = 0x00C24388;

// В будущем сюда будут добавляться другие адреса и смещения:
// constexpr uintptr_t PLAYER_GUID_ADDR = 0x...;
// constexpr uintptr_t PLAYER_COORDS_ADDR = 0x...;

}  // namespace Offsets